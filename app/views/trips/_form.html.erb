<%= form_with(model: trip, local: true, :html => {:class => "form-horizontal center"}) do |form| %>
  <% if trip.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(trip.errors.count, "error") %> prohibited this trip from being saved:</h2>

      <ul>
      <% trip.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <p><%= form.label :title, "Title of the trip", class: "control-label" %></p>
    </p><%= form.text_area :title, class: "control-label" %></p>
  </div>

  <div class="form-group">
    <p><%= form.label :summary, "Summary of the trip" %></p>
    <p><%= form.text_area :summary %></p>
  </div>

  <div class="form-group">
    <p><%= form.submit 'Create the trip', :class => 'btn btn-success' %></p>
  </div>
<% end %>
