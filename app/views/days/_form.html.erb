<%= form_with(model: day, local: true, :html => {:class => "form-horizontal center"}) do |form| %>
  <% if day.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(day.errors.count, "error") %> prohibited this day from being saved:</h2>

      <ul>
      <% day.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <p><%= form.label :title, "Title of the day" %></p>
    </p><%= form.text_area :title, {class: "form-control"} %></p>
  </div>

  <div class="form-group">
    <p><%= form.label :description, "Description of the day" %></p>
    <p><%= form.text_area :description, {class: "form-control"} %></p>
  </div>

  <div class="form-group">
    <p><%= form.submit 'Submit', {class: 'btn btn-success'} %></p>
  </div>
<% end %>
